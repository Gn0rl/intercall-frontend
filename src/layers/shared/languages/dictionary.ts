import en from '../../shared/languages/en.json'
import ru from '../../shared/languages/ru.json'
import { Language } from '../types'

export const dictionary: Record<Language, Record<string, string>> = {
    en,
    ru,
}
